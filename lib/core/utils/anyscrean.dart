import 'package:flutter/material.dart';

class AnyScrean extends StatelessWidget {
  const AnyScrean({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Column(children: [Center(child: Text("data"))]),
    );
  }
}
